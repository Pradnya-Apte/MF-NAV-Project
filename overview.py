import datetime
import cx_Oracle

def all_time(investor):
    #conn=''
    #cursor = conn.cursor()

    time=[x for x in range(2011,)]

    today = datetime.date.today()
    year = today.year
    n=year + 1
    yearly_value=[]

    for i in range(2011,n):
        query=('')
        #cursor.execute('SELECT x, y FROM graph_data')
        #data = cursor.fetchall()
        #conn.close()

        
    


all_time('RDA')





